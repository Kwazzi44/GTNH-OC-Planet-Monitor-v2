-- =============================================================================
-- node/node_config.lua — Node Configuration
-- =============================================================================
-- Скопируй на каждый Node-компьютер. Заполни planet_name и redstone_restart.
-- Список машин обнаруживается АВТОМАТИЧЕСКИ через GT Adapter.
-- Для ПЕРЕЗАПУСКА используется Redstone I/O сигнал.

local config = {}

-- ─── ОБЯЗАТЕЛЬНО: имя планеты ─────────────────────────────────────────────
config.planet_name = "MyPlanet"   -- <-- замени на имя своей планеты

-- ─── Редстоун-карта для перезапуска машин ─────────────────────────────────
-- Ключ   = название машины (как показывает авто-скан, смотри логи Node при запуске)
-- Значение = таблица настроек сигнала:
--   side          — сторона Redstone I/O блока, смотрящая на контроллер
--                   0=down, 1=up, 2=north, 3=south, 4=west, 5=east
--   color         — (опционально) цвет bundled-кабеля (0-15), nil = обычный редстоун
--   pulse         — (опционально) длительность импульса в секундах (по умолчанию 0.5)
--   mode          — "pulse"    : кратковременный импульс HIGH→LOW  (по умолчанию)
--                   "enable"   : выставить HIGH и оставить (машина включается пока сигнал есть)
--                   "toggle"   : LOW→HIGH→LOW (сброс)
--
-- Пример физической схемы:
--   [Redstone I/O] → (side=2, NORTH) → редстоун-провод → [Контроллер EBF]
--   [Redstone I/O] → (side=3, SOUTH) → редстоун-провод → [Контроллер Distillation]

config.redstone_restart = {

  -- ── Пример 1: Обычный редстоун-импульс ──────────────────────────────────
  -- ["Electric Blast Furnace"] = {
  --   side  = 2,       -- NORTH сторона Redstone I/O
  --   mode  = "pulse",
  --   pulse = 0.5,     -- 0.5 секунды
  -- },

  -- ── Пример 2: Bundled cable (Project Red / covers) ──────────────────────
  -- ["Distillation Tower"] = {
  --   side  = 3,       -- SOUTH сторона Redstone I/O
  --   color = 0,       -- белый провод (0=white, 1=orange, ... 15=black)
  --   mode  = "pulse",
  -- },

  -- ── Пример 3: Режим "enable" (машина работает пока сигнал HIGH) ─────────
  -- ["Vacuum Freezer"] = {
  --   side  = 4,       -- WEST
  --   mode  = "enable",
  -- },

}

-- ─── Переопределение имён машин (опционально) ─────────────────────────────
-- Если авто-определённое имя машины неудобно, можно задать своё по addr:
-- config.name_overrides = {
--   ["aabb-ccdd-1234"] = "My EBF",
-- }
config.name_overrides = {}

-- ─── Сеть ────────────────────────────────────────────────────────────────
config.modem_port         = 42
config.heartbeat_interval = 30

return config
