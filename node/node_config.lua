-- =============================================================================
-- node/node_config.lua — Node Configuration Template
-- =============================================================================
-- Скопируй этот файл на компьютер каждой планеты.
-- Заполни ТОЛЬКО поле planet_name — название этой планеты.
-- Список машин обнаруживается АВТОМАТИЧЕСКИ через GT Adapter.

local config = {}

-- ─── ОБЯЗАТЕЛЬНО: имя планеты ─────────────────────────────────────────────
config.planet_name = "MyPlanet"   -- <-- замени на имя своей планеты

-- ─── Опциональные переопределения имён машин ──────────────────────────────
-- Если авто-определённое имя машины тебя не устраивает,
-- можно переопределить по адресу компонента:
-- config.name_overrides = {
--   ["aabb-ccdd-1234-5678"] = "My Custom EBF Name",
-- }
config.name_overrides = {}

-- ─── Fallback: Redstone I/O (если GT Adapter не умеет enable()) ───────────
-- Если для какой-то машины restart через adapter не работает,
-- можно указать сторону Redstone I/O по адресу компонента:
-- config.redstone_fallback = {
--   ["aabb-ccdd-1234-5678"] = { rs_side = 0 },   -- 0=down,1=up,...
-- }
config.redstone_fallback = {}

-- ─── Сеть ─────────────────────────────────────────────────────────────────
config.modem_port    = 42      -- Должен совпадать с Hub
config.heartbeat_interval = 30 -- Секунды между принудительными ALERT'ами если что-то упало

return config
